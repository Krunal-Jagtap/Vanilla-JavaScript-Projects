// Variables - Use for Store value
// Variables name prefer in camelCase

const userName = "Krunal";
console.log(userName);

const _sendMessage = "Hi there we are learning JavaScript";
console.log(_sendMessage);

const $required = "Consistency";
console.log($required);
